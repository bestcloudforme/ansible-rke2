---
- name: Check if swap is enabled
  ansible.builtin.command: swapon --show
  register: swap_status
  changed_when: false

- name: Disable swap
  ansible.builtin.command: swapoff -a
  when: swap_status.stdout | length > 0
  changed_when: true

- name: Remove swap from fstab
  ansible.builtin.lineinfile:
    path: /etc/fstab
    regexp: '\sswap\s'
    state: absent

- name: Configure system resource limits
  ansible.builtin.copy:
    dest: /etc/security/limits.d/99-rke2.conf
    mode: '0644'
    content: |
      * soft nofile {{ preflight_nofile_limit }}
      * hard nofile {{ preflight_nofile_limit }}
      * soft nproc {{ preflight_nproc_limit }}
      * hard nproc {{ preflight_nproc_limit }}
      root soft nofile {{ preflight_nofile_limit }}
      root hard nofile {{ preflight_nofile_limit }}

- name: Load required kernel modules
  community.general.modprobe:
    name: "{{ item }}"
    state: present
  loop: "{{ _preflight_modules }}"

- name: Persist kernel modules across reboots
  ansible.builtin.copy:
    dest: /etc/modules-load.d/rke2.conf
    mode: '0644'
    content: |
      {% for mod in _preflight_modules %}
      {{ mod }}
      {% endfor %}

- name: Apply sysctl parameters
  ansible.posix.sysctl:
    name: "{{ item.key }}"
    value: "{{ item.value }}"
    sysctl_file: /etc/sysctl.d/99-rke2.conf
    reload: true
    state: present
  loop: "{{ preflight_sysctl_params | dict2items }}"
  loop_control:
    label: "{{ item.key }}"

# kubelet validates these under protect-kernel-defaults instead of setting them,
# and they do not survive a reboot on their own.
- name: Persist the kernel parameters kubelet validates
  ansible.posix.sysctl:
    name: "{{ item.key }}"
    value: "{{ item.value }}"
    sysctl_file: /etc/sysctl.d/60-rke2-cis.conf
    reload: true
    state: present
  loop: "{{ preflight_kernel_defaults_sysctl | dict2items }}"
  loop_control:
    label: "{{ item.key }}"
  when: rke2_protect_kernel_defaults | bool or rke2_cis_profile | default('') | length > 0

# RKE2 looks up both the user and the group, and only creates the group as a
# side effect of USERGROUPS_ENAB.
- name: Create etcd group for CIS compliance
  ansible.builtin.group:
    name: etcd
    system: true
  when: rke2_cis_profile | default('') | length > 0

- name: Create etcd user for CIS compliance
  ansible.builtin.user:
    name: etcd
    group: etcd
    system: true
    shell: /sbin/nologin
    home: /var/lib/etcd
    create_home: false
  when: rke2_cis_profile | default('') | length > 0

- name: Configure container log rotation
  ansible.builtin.copy:
    dest: /etc/logrotate.d/container-logs
    mode: '0644'
    content: |
      /var/log/containers/*.log
      /var/log/pods/*/*.log {
          daily
          rotate 7
          compress
          missingok
          notifempty
          maxsize {{ preflight_logrotate_maxsize | default('250M') }}
          copytruncate
      }
